apiVersion: v1
kind: Service
metadata:
  name: flag-service
  namespace: feature-flags
spec:
  type: ClusterIP
  selector:
    app: flag-service
  ports:
    - port: 8002
      targetPort: 5000
      protocol: TCP
