apiVersion: v1
kind: Service
metadata:
  name: targeting-service
  namespace: feature-flags
spec:
  type: ClusterIP
  selector:
    app: targeting-service
  ports:
    - port: 8003
      targetPort: 5000
      protocol: TCP
