name: "CI — evaluation-service"

on:
  push:
    branches: ["main"]
    paths:
      - "evaluation-service/**"
      - ".github/workflows/ci-evaluation-service.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "evaluation-service/**"
      - ".github/workflows/ci-evaluation-service.yml"

permissions:
  contents: read
  security-events: write

env:
  GO_VERSION: "1.21"
  AWS_REGION: "us-east-1"
  ECR_REPOSITORY: "pos2/evaluation-service"

jobs:
  # ───────────────────────────────────────────────────────────────────────────
  # JOB 1 — Build & Unit Test
  # ───────────────────────────────────────────────────────────────────────────
  build-test:
    name: "Build & Unit Test"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: evaluation-service

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go ${{ env.GO_VERSION }}
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: evaluation-service/go.sum

      - name: Download dependências
        run: go mod download

      - name: Build
        run: go build ./...

      - name: Unit Tests
        run: go test ./... -v -race -coverprofile=coverage.out

      - name: Upload cobertura de testes
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-evaluation-service
          path: evaluation-service/coverage.out
          if-no-files-found: ignore

  # ───────────────────────────────────────────────────────────────────────────
  # JOB 2 — Linter / Static Analysis
  # ───────────────────────────────────────────────────────────────────────────
  lint:
    name: "Linter / Static Analysis"
    runs-on: ubuntu-latest
    needs: build-test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go ${{ env.GO_VERSION }}
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: evaluation-service/go.sum

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          working-directory: evaluation-service
          args: --timeout=5m

  # ───────────────────────────────────────────────────────────────────────────
  # JOB 3 — Security Scan (SCA + SAST)
  # ───────────────────────────────────────────────────────────────────────────
  security-scan:
    name: "Security Scan (SCA + SAST)"
    runs-on: ubuntu-latest
    needs: build-test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go ${{ env.GO_VERSION }}
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: evaluation-service/go.sum

      - name: "SCA — Trivy (dependências, formato SARIF)"
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: "fs"
          scan-ref: "evaluation-service"
          scanners: "vuln"
          format: "sarif"
          output: "trivy-sca.sarif"
          severity: "CRITICAL"
          exit-code: "1"
          ignore-unfixed: true

      - name: "Upload SARIF — Trivy SCA"
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        continue-on-error: true
        with:
          sarif_file: trivy-sca.sarif
          category: trivy-sca-evaluation-service

      - name: Instalar gosec
        run: go install github.com/securego/gosec/v2/cmd/gosec@latest

      - name: "SAST — gosec (bloqueio em HIGH/CRITICAL)"
        working-directory: evaluation-service
        run: |
          gosec -fmt json -out gosec.json ./... || true
          python3 << 'PYEOF'
          import json, sys, os
          if not os.path.exists('gosec.json'):
              print("gosec.json nao encontrado — ignorando"); sys.exit(0)
          data = json.load(open('gosec.json'))
          issues = data.get('Issues') or []
          bloqueantes = [
              i for i in issues
              if i.get('severity', '').upper() in ('HIGH', 'CRITICAL')
          ]
          if bloqueantes:
              print(f"[BLOQUEADO] {len(bloqueantes)} issue(s) HIGH/CRITICAL encontrado(s):")
              for i in bloqueantes:
                  print(f"  [{i['severity']}] {i['rule_id']}: {i['details']}")
                  print(f"    Arquivo: {i['file']}:{i['line']}")
              sys.exit(1)
          print(f"SAST OK — {len(issues)} issue(s) de baixa/media severidade (nao bloqueiam)")
          PYEOF

  # ───────────────────────────────────────────────────────────────────────────
  # JOB 4 — Docker Build, Container Scan & Push para ECR
  # ───────────────────────────────────────────────────────────────────────────
  docker:
    name: "Docker Build, Scan & Push"
    runs-on: ubuntu-latest
    needs: [lint, security-scan]
    outputs:
      tag: ${{ steps.meta.outputs.tag }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Gerar tag da imagem
        id: meta
        run: |
          SHORT_SHA="${GITHUB_SHA:0:7}"
          echo "tag=v1.0.0-${SHORT_SHA}" >> "$GITHUB_OUTPUT"
          echo "local_image=evaluation-service:${SHORT_SHA}" >> "$GITHUB_OUTPUT"

      - name: Docker Build
        run: |
          docker build \
            --tag "${{ steps.meta.outputs.local_image }}" \
            --label "git.sha=${{ github.sha }}" \
            --label "git.ref=${{ github.ref_name }}" \
            evaluation-service/

      - name: "Container Scan — Trivy (imagem Docker)"
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: "image"
          image-ref: ${{ steps.meta.outputs.local_image }}
          format: "table"
          exit-code: "1"
          severity: "CRITICAL"
          ignore-unfixed: true

      - name: Configurar credenciais AWS
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login no Amazon ECR
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Tag e Push para ECR
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          REGISTRY="${{ steps.login-ecr.outputs.registry }}"
          TAG="${{ steps.meta.outputs.tag }}"
          LOCAL="${{ steps.meta.outputs.local_image }}"

          docker tag "${LOCAL}" "${REGISTRY}/${{ env.ECR_REPOSITORY }}:${TAG}"
          docker tag "${LOCAL}" "${REGISTRY}/${{ env.ECR_REPOSITORY }}:latest"
          docker push "${REGISTRY}/${{ env.ECR_REPOSITORY }}:${TAG}"
          docker push "${REGISTRY}/${{ env.ECR_REPOSITORY }}:latest"

          echo "Imagem publicada: ${REGISTRY}/${{ env.ECR_REPOSITORY }}:${TAG}"

  # ───────────────────────────────────────────────────────────────────────────
  # JOB 5 — Atualizar tag da imagem no repositório GitOps
  # ───────────────────────────────────────────────────────────────────────────
  update-gitops:
    name: "Atualizar tag no GitOps"
    runs-on: ubuntu-latest
    needs: docker
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Configurar git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Atualizar tag da imagem no GitOps
        run: |
          TAG="${{ needs.docker.outputs.tag }}"
          FILE="gitops/apps/evaluation-service/deployment.yaml"
          sed -i "s|pos2/evaluation-service:.*|pos2/evaluation-service:${TAG}|g" "${FILE}"
          echo "Tag atualizada para: ${TAG}"
          grep "image:" "${FILE}"

      - name: Commit e Push
        run: |
          TAG="${{ needs.docker.outputs.tag }}"
          git add gitops/apps/evaluation-service/deployment.yaml
          if ! git diff --staged --quiet; then
            git commit -m "ci(gitops): atualiza evaluation-service para ${TAG} [skip ci]"
            git push
          else
            echo "Nenhuma alteracao detectada — tag ja e a mais recente"
          fi
